# Vite proxies local /api calls to this live API, avoiding browser CORS during
# frontend-only development. Set it to http://127.0.0.1:5000 to use a local API.
VITE_DEV_API_PROXY=https://api.chalakgo.com
